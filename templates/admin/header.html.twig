<div class="main_nav_container">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-right">
                <nav class="navbar">
                    <ul class="navbar_menu">
                        <li><a href="{{ path('admin_products') }}">Accueil</a></li>
                        <li><a href="#">Liste des articles</a></li>
                        <li><a href="#">Liste des clients</a></li>
                        <li><a href="#">Liste des commandes</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                    <ul class="navbar_user">
                        {% if not app.user and is_granted('ROLE_ADMIN') == false %}
                            <li><a href="{{ path('account_admin_login') }}"><i class="fa fa-user" aria-hidden="true"></i></a></li>
                        {% endif %}
                       
                        {% if app.user and is_granted('ROLE_ADMIN') %}
                        
                        <li><a href="{{ path('account_admin_logout') }}"><i class="fa fa-sign-out" aria-hidden="true"></i></a></li>
                    </ul>

                    {% endif %}
                    <div class="hamburger_container">
                        <i class="fa fa-bars" aria-hidden="true"></i>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</div>
