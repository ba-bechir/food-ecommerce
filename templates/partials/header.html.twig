<div class="main_nav_container">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-right">
                <nav class="navbar">
                    <ul class="navbar_menu">
                        <li><a href="{{ path('products') }}">Accueil</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('admin_products') }}">Liste des articles</a></li>
                            <li><a href="#">Liste des clients</a></li>
                            <li><a href="#">Commandes</a></li>
                            <li><a href="#">Contact</a></li>
                        {% endif %}
                    </ul>
                    <ul class="navbar_user">
                        <li><a href="{{ path('account_login') }}"><i class="fa fa-user" aria-hidden="true"></i></a></li>
                       
                        {% if app.user %}

                        <li class="checkout">
                            <a href="{{ path('cart_connected', {'id' : app.user.id }) }}">
                
                                <i class="fa fa-shopping-cart" aria-hidden="true"></i>
                            </a>
                        </li>
                        
                        <li><a href="{{ path('account_logout') }}"><i class="fa fa-sign-out" aria-hidden="true"></i></a></li>
                    </ul>

                    {% endif %}
                    <div class="hamburger_container">
                        <i class="fa fa-bars" aria-hidden="true"></i>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</div>